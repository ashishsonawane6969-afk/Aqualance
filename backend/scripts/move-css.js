// LEGACY FILE — safe to delete. This was a one-off CSS migration script from the MPA→SPA transition.
// All CSS files were already moved. This script is not referenced anywhere and is no longer needed.
